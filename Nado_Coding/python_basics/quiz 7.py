for r in range(1, 51):
    with open("week {0} report.txt".format(r), "w", encoding="utf8") as report:
        report.write("- week {0} report-".format(r))
        report.write("\ndepartment: ")
        report.write("\nname ")
        report.write("\nreport: ")